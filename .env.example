# KrishiRakshak — Environment Variables
# Copy this file to .env and update values as needed

# API Gateway URL (from Lambda deployment)
# Get this from AWS Console → API Gateway → Stages → prod → Invoke URL
VITE_API_GATEWAY_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod

# AWS Region
VITE_AWS_REGION=ap-south-1

# App version
VITE_APP_VERSION=1.0.0

# Demo mode — controls whether mock data is used instead of real AWS backend
# LOCAL development: keep this as "true" (uses built-in mock data, no AWS needed)
# VERCEL production: set to "false" to call real AWS Bedrock/Rekognition APIs
# NOTE: VITE_API_GATEWAY_URL must also be set for live mode to work
VITE_DEMO_MODE=true
